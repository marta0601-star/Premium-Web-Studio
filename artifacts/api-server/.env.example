# See ../../.env.example in the repo root for the canonical list.
# This copy lives next to api-server for local-dev convenience
# (e.g. `cd artifacts/api-server && cp .env.example .env`).

APP_PASSWORD=change-me-in-production
COOKIE_SECRET=change-me-random-string

ALLEGRO_CLIENT_ID=
ALLEGRO_CLIENT_SECRET=
# ALLEGRO_REDIRECT_URI=

# TOKEN_STORE_PATH=

# SELLER_CITY=
# SELLER_POSTCODE=
# SELLER_STATE=

PORT=3000
